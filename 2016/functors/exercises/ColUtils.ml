let todo _ = failwith "Ups, you forgot to implement something."

open Collection

module ColUtils (C: Collection) = struct
        let filter p a = todo ()
        let for_all f a = todo ()
        let length a = todo ()
        let exists p a = todo ()
        let merge a b = todo ()
        let tuple a b = todo ()
        let every_nth a n = todo ()
end
